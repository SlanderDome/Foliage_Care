services:
  - name: foliage-backend
    type: web
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn Backend.main:app --host 0.0.0.0 --port $PORT"
    pythonVersion: 3.11.9
